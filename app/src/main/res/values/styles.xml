<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Splash Screen -->
    <style name="SplashScreenTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimaryDark">@color/colorPrimary</item>
    </style>

    <!-- Primary TextView Detail Style -->
    <style name="PrimaryTextDetailStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:ellipsize">end</item>
        <item name="android:padding">@dimen/padding</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <!-- Secondary TextView Detail Style -->
    <style name="SecondaryTextDetailStyle" parent="PrimaryTextDetailStyle">
        <item name="android:textAppearance">?android:textAppearanceSmall</item>
        <item name="android:textColor">@color/secondary_text</item>
    </style>

    <!-- Description Title Detail Style -->
    <style name="DescTitleDetailStyle" parent="PrimaryTextDetailStyle">
        <item name="android:textColor">@color/colorDescDetailActivity</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Description Body Detail Style -->
    <style name="DescBodyDetailStyle" parent="SecondaryTextDetailStyle">
        <item name="android:maxLines">20</item>
        <item name="android:textColor">@color/colorDescDetailActivity</item>
    </style>

    <!-- Book Detail Style -->
    <style name="BookDetailStyle" parent="SecondaryTextDetailStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorDescDetailActivity</item>
    </style>

    <!-- Link Detail -->
    <style name="LinkDetailStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/margin_button</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@color/colorPriceBackground</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/padding</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:textColor">@color/colorPriceText</item>
    </style>

    <!-- Price Book Style -->
    <style name="PriceBookStyle">
        <item name="android:layout_width">@dimen/pricing_size</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_alignParentEnd">true</item>
        <item name="android:layout_marginEnd">@dimen/margin_price</item>
        <item name="android:layout_marginTop">@dimen/margin_price</item>
        <item name="android:background">@color/colorPriceBackground</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/padding_price</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPriceText</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
